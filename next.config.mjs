/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["c.ndtvimg.com", "foodish-api.com"]
  },
  trailingSlash: true,
  reactStrictMode: false,
};

export default nextConfig;
